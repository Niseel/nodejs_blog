<div class="mt-4">
    <h2>{{post.title}}</h2>
    <p style="color: #666">{{post.author}} | {{post.createAt}}</p>
    <img src="{{post.image}}" alt="{{post.title}}">
    <p>{{post.content}}</p>
</div>